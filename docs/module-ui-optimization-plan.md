# 模块内容与 UI 优化计划

更新时间：2026-09-22（第九轮）
状态：第一~八轮按模块盘点已实施；第九轮修复中转拓扑的落地计数口径，已完成并经浏览器复验

## 已完成修复

### 第九轮（本轮，中转拓扑落地计数）

问题：拓扑图徽标显示"落地 7"，下方列表只有 5 行；徽标与列表口径不同，且脏地域标签被当成国家计数。

- [x] 口径统一：拓扑徽标、右侧列表、面板标题、总览"国家覆盖/覆盖国家"全部改为同一次 `getCountryStats()` 结果，经同一个"能否落到地图点位"判定（共享 `splitMappableCountryStats`）分流，不再各自取数 — P0
- [x] 未识别地域不再冒充国家：拓扑以"未识别地域 N"虚线徽标、列表尾注与总览"· 未识别 N 个"单独说明，不计入落地/覆盖国家 — P0
- [x] 计数补单位与语义："落地国家 N 个""入口区域/中转节点"，并在说明里点明三个计数都是地点数、不是机器数 — P1
- [x] 超出展示上限时补充"另有 N 个国家未展示"，避免列表行数被误读为国家总数 — P1
- [x] 字典补齐：美国入口增加常用机场/城市三字码与中文名（IAD/SJC/LAX/ORD/Santa Clara/硅谷 等），新加坡补 `SG` 别名，`IAD` 这类标签归一到"美国"而非新国家 — P0
- [x] 新增回归测试 `test/country-stats-mapping.test.js`：锁定"同城多标签合并为一国""无法识别标签不计为落地国家"两条不变量

说明：本地演示数据里仍保留 `region=IAD` 与 `region` 为空的失效节点，作为脏标签回归样本；本机想停止对失效节点探测，用 `AUTO_PROBE_ENABLED=false` 启动。

### 第八轮（任务中心专项）

审计快照：`.impeccable/critique/2026-09-22T12-54-36Z__public-tasks-html.md`（评分 16/40，3×P0 + 2×P1）

- [x] 自动巡检从整块面板降级为一行状态条，首屏主位交还任务池 — P0
- [x] 任务池实时性：仅在存在 `queued/running` 任务时每 15s 轮询；搜索输入防抖并恢复焦点与光标位置 — P0
- [x] 探测诊断口径统一：删除页面自造的 reason/stage 映射，改用 `public/js/shared/probe-formatters.js`（新增 `probeStageLabel`）单一来源，并去重“原始错误” — P0
- [x] 键盘与焦点可达：行改为 `tabindex="0"` + 描述性 `aria-label`（不再滥用 `role="button"`），Enter/Space 选中并带 IME 组合守卫，重渲染后按数据属性恢复焦点；`:focus:not(:focus-visible)` 不再吞掉焦点环 — P1
- [x] “重新初始化”增加显式二次确认，说明会重启 `sshd`、覆写 `/etc/airport/node.env`、可能覆盖既有配置，并点名目标节点 — P1
- [x] 表格列宽状态驱动：宽度从内联样式移入 CSS 类，详情栏展开时隐藏“说明”列并把宽度让给扫描列（≥1101px），避免动作按钮截断
- [x] 复探按钮文案收敛（复探中 / 立即复探 / 复探），加载态不再撑破动作列
- [x] 首屏“数据时间”基线初始化，不再出现空白新鲜度

未纳入本轮（明确降级到后续）：巡检轮次批量聚合视图、任务详情页、取消/重跑与执行租约。

### 第七轮（P2 收尾）
- [x] 全局加载骨架屏：页面渲染前显示 spinner + "正在加载控制台数据…"（page-render-runtime + layout.css）
- [x] 协议模板"校验模板 JSON"按钮：语法校验 + 结构化预览（proxy-profiles-page）
- [x] 系统模板"检查脚本"按钮：shebang/行数/CRLF/占位符检查（system-templates-page）
- [x] 注册令牌"调整上限"编辑入口：过期/用尽令牌可直接 PATCH max_uses（tokens-page）

### 第六轮（已完成）
（见上文）
- [x] 任务中心状态筛选补充"排队中 queued"选项（与 pending 统计口径对齐）
- [x] 任务中心触发方式中文映射（bootstrap_register → "bootstrap 注册"等 10 种）
- [x] tokens/shell 复制按钮文案粘滞修复（1.6s 后恢复原文案）

### 第五轮（已完成）
（见上文）
- [x] 发布中心逐节点发布详情：新增"逐节点"展开行（deployments 状态/角色/引擎/digest/路径/说明）+ CSS
- [x] Shell 会话关闭增加确认（提示会话中进程可能被终止）
- [x] 注册令牌停用增加确认（提示无法再注册新节点）
- [x] 系统用户下发历史按选中用户过滤（不再只显示全局最近 8 条）
- [x] 终端点击历史滚动到回显面板（第四轮）
- [x] 路由页 nodeTable 死参数清理 + 中转/直连列表上游信息提示（第四轮）
- [x] 厂商表单提交 loading + 防重复（第四轮）

### 第四轮（已完成）
（见上文）
- [x] 终端点击执行历史自动滚动到回显面板（terminal-output-panel）
- [x] 路由页 nodeTable 死参数清理（showCommercial/showRoute），中转/直连列表补充上游信息提示
- [x] 厂商表单提交 loading + 防重复（finally 恢复按钮文案）

### 第三轮（已完成）
（见上文）
- [x] 无效节点 id 静默回退第一台 → 改为返回 null + 区分"无节点/节点不存在"文案（node-navigation.js + 详情页/Shell 页）
- [x] 节点详情"最近中转诊断"标签错配（实为探测摘要）→ "最近中转探测"
- [x] 删除 commercialOverview 重复死代码（与 costOverview 9 行重复且无人渲染）
- [x] 总览成本空态语义统一（闲置成本 "0" → "待补"，避免误导为真实 0 成本）
- [x] 总览巡检卡异常时增加"去任务中心"跳转按钮
- [x] 资产弹窗 Escape 关闭增加 open 状态检查（不再无条件触发）
- [x] 资产表单提交防重复（loading + 禁用）
- [x] 节点清单头部术语统一（"可接管/状态稳定" → "可用节点/状态可用"）
- [x] 节点清单筛选 chips 增加"已过期 N 台"
- [x] 厂商"同步云资源"占位按钮 success → info 色调 + 更诚实文案

### 前两轮（已完成）
（见上文）

### 全局骨架
- [x] 死按钮处理：任务中心"创建任务模板/新建一次性任务"→"批量执行/管理系统模板"跳转；令牌页"查看审计日志"移除
- [x] 侧栏导航分组：节点运维 / 配置发布 / 系统管理 三组
- [x] 品牌标识：文字 "AC" → 登录页同款 SVG 图形
- [x] `statusText` 补 draft 分支（草稿模板不再显示"待初始化"）— 跨模块 P0

### 发布中心
- [x] 提交按钮防重复（loading + 禁用 + "发布中..."）
- [x] "自动回滚"徽标 → "可回滚"+tooltip（避免误导）
- [x] 筛选支持中文状态词（输入"执行中"可匹配 running）
- [x] change_summary 不再顶掉"接入用户/节点组"范围信息
- [x] 空状态区分"无数据/无匹配"
- [x] 筛选防抖（防止清空发布表单）

### 协议模板
- [x] 字段按协议/传输/安全联动显隐（HY2 混淆仅 hysteria2、Flow 仅 vless、Path/Host 按传输层、Early Data 仅 ws、TLS/Reality 材料按安全层）— P0
- [x] 筛选防抖（防止清空编辑器）

### 接入用户
- [x] 到期文案逻辑修复（按 expires_at 而非 updated_at）
- [x] alterId 字段仅 vmess 显示
- [x] 死绑定修复（focus-access-user-form → access-user-create-empty）
- [x] 筛选防抖

### 节点清单
- [x] 搜索框失焦修复（防抖 + 焦点/光标恢复）— P0
- [x] "即将到期"口径排除已过期（days >= 0）
- [x] 筛选空态（区分无数据/无匹配）
- [x] 节点名 HTML 转义

### 总览
- [x] "即将到期"口径排除已过期
- [x] "最近事件"→"最近活跃节点"（名实相符）
- [x] 节点名 HTML 转义

### 任务中心
- [x] 搜索框失焦修复（防抖 + 焦点/光标恢复）— P0
- [x] 触发参数对象序列化（不再显示 [object Object]）— P0
- [x] reset 时清理防抖定时器

### 批量终端
- [x] 提交前置中文校验（未选节点/空命令/空脚本）+ loading 防重复 — P0
- [x] 预设脚本占位符修复（PANEL_TOKEN=replace_me 移除，改为空值+警告注释）

### 系统用户 / 系统模板
- [x] 表单提交/批量下发 loading 态（防重复提交）
- [x] "尚未下发"死文案 → "暂无更新记录"
- [x] 空状态区分
- [x] "新建空白系统模板" → "新建示例模板"（实际预填 hello 脚本）

### 注册令牌
- [x] 展示 created_at / last_used_node_id（满足"必须有审计"的页面承诺）

### 中转拓扑
- [x] tooltip / inline-error 深字深底对比度修复（routes.css）— P1
- [x] 落地计数口径与地域字典归一（第九轮，详见上文）

## 已确认但未修复（下一轮）

- 任务中心：巡检轮次批量聚合视图、任务详情页、取消/重跑与执行租约
- 接入用户：分享 warnings 前置；“生效发布”可点击；凭证复制按钮
- 协议模板：相关发布可点击跳转；下拉状态标注；兼容矩阵与后端同源
- Shell：`exit_code`/transport 展示补齐；空闲轮询降频
- 厂商：`provider_id` 优先匹配
- 全局：原生 `alert/confirm` 内联化（当前 7 处破坏性动作仍用 `window.confirm`）
- 中转拓扑：`ensureEntryPoint` 在节点未填 `entry_region` 时静默默认成"中国大陆"，入口区域计数因此偏高，需要改为显式标注"未填入口"（第九轮发现，本轮未动）

说明：原清单中的发布中心逐节点详情、注册令牌审计字段、系统用户下发过滤、Shell 关闭确认、厂商占位按钮色调、全局骨架屏已在第三/五/七轮完成。

## 验证状态

- [x] 全部修改 JS 语法检查通过（`node --check`）
- [x] 本地服务 + 真实数据页面复验（第八轮：浏览器实测选中态列宽、键盘焦点恢复、确认文案、诊断口径、轮询与防抖）
- [x] 本地页面复验（第九轮：中转拓扑徽标"落地国家 5 / 未识别地域 1"与右侧列表 5 行一致；`IAD` 归入美国）
- [x] `npm test` 84 项通过（含第九轮新增 `test/country-stats-mapping.test.js` 3 项）
- [ ] 桌面/移动端关键流程自动化浏览器测试（仍缺）

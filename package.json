{
  "name": "airport-control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight multi-cloud node control plane",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "find src public/js scripts test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "seed": "node scripts/seed-local-demo.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
